# !! DEV ONLY — plaintext is acceptable for dev !!
# For staging/prod: encrypt with SealedSecrets or SOPS before committing.
apiVersion: v1
kind: Secret
metadata:
  name: vault-bootstrap-secrets
  namespace: vault
type: Opaque
stringData:
  env: "dev"
  db-host: "dev-db.internal"
  db-password: "dev_pass_123"
  api-key: "dev-api-key-abc"
  enable-audit: "false"
