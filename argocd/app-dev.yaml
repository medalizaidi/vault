apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: vault-dev
  namespace: argocd
  labels:
    env: dev
    app: vault
spec:
  project: default
  sources:
    # 1. Vendored Vault Helm chart with dev values
    - repoURL: https://github.com/<your-org>/vault-gitops
      targetRevision: main
      path: charts/vault
      helm:
        releaseName: vault
        valueFiles:
          - values.yaml
          - ../../envs/values-dev.yaml

    # 2. RBAC + init/unseal/bootstrap jobs
    - repoURL: https://github.com/<your-org>/vault-gitops
      targetRevision: main
      path: jobs/init-unseal

    - repoURL: https://github.com/<your-org>/vault-gitops
      targetRevision: main
      path: jobs/bootstrap

    # 3. Dev-specific bootstrap secret
    - repoURL: https://github.com/<your-org>/vault-gitops
      targetRevision: main
      path: envs/dev

  destination:
    server: https://<AKS-DEV-API-SERVER>   # replace with actual AKS dev API URL
    namespace: vault

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
